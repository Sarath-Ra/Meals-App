import 'package:flutter/material.dart';

class FavouitesScreen extends StatelessWidget {
  const FavouitesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Favourites"),);
  }
}